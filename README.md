# ホットラインAIエージェント ローカルプロトタイプ

GitHub Pages から切り離した、ローカル起動用の独立プロトタイプです。
React、Vite、Babel、CDN、npm install は不要です。

## 起動方法

### 方法1：もっとも簡単

`index.html` をブラウザで開いてください。

### 方法2：ローカルサーバーで起動

Node.js が入っている環境で、以下を実行してください。

```bash
npm start
```

起動後、ブラウザで以下を開きます。

```text
http://localhost:3000
```

Windows の場合は `start-windows.bat` をダブルクリックしても起動できます。
Mac / Linux の場合は以下です。

```bash
./start-mac-linux.sh
```

## ファイル構成

```text
hotline_ai_agent_local/
├─ index.html
├─ style.css
├─ app.js
├─ server.js
├─ package.json
├─ start-windows.bat
├─ start-mac-linux.sh
└─ README.md
```

## この版の仕様

- 画面全体をチャットボットUIとして構成
- ユーザーが見る範囲は「会話履歴」と「回答操作エリア」のみ
- 操作対象は画面下部の青枠内に固定
- 大きな文字、大きなボタン、明確な操作導線
- 店番6桁バリデーション
- 回答に応じた動的分岐
- AI要約確認フロー
- 項目単位の修正フロー
- 電話相談へのフォールバック
- Slack通知、管理表転記はモック表示

## 注意

このプロトタイプは外部送信を行いません。
入力内容はブラウザ画面上でのみ扱われます。
実運用でSlack、Google Sheets、LLM APIと接続する場合は、バックエンド実装と認証・権限設計が別途必要です。


## v5 UIUX変更点

- 画面上部60%をチャットボット会話エリア、下部40%をユーザー選択・入力エリアに固定しました。
- 会話エリアと入力エリアは、それぞれ独立して縦スクロールできます。
- ヘッダー背景をグリーン、文字を白に変更しました。
- AIの案内文を「安心してください」「それはお困りですね」など、より温かく寄り添う口調に調整しました。
